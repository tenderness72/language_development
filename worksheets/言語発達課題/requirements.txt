# 関連語の想起課題ジェネレータ — 実行に必要な依存（完全ローカル・ネットワーク不使用）
# インストール:  pip install -r requirements.txt

# PDF生成（日本語TTF埋め込み）
reportlab>=4.0

# data/*.yaml の読み込み
PyYAML>=6.0

# --- 任意 ---------------------------------------------------------------
# 同梱フォント(fonts/NotoSansJP-*.ttf)を可変フォントから再生成する場合のみ必要。
# 通常の生成では不要（静的TTFは同梱済み）。
# fonttools>=4.40
